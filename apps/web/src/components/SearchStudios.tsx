import { Box, Input, Typography } from '@mui/material';

export default function SearchStudios() {
  return (
    <Box margin={'auto'}>
      <Typography>Find studios in your region</Typography>
      <Input />
    </Box>
  );
}
