import { FallbackMessage } from '@studiobooker/utils';

export default function ServiceDetailStart() {
  return (
    <FallbackMessage message="Select category and service to view details..." />
  );
}
